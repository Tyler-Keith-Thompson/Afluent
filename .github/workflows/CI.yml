name: CI

on: [ push, workflow_dispatch ]

jobs:
  test:
    runs-on: macos-13
    steps:
      - uses: actions/checkout@v2
        with:  
            fetch-depth: 0  
      - name: Run TESTS
        run: swift test